name: Slack Notification
on:
  push:
    branches:
      - feat/slacknotification

jobs:
  send:
    runs-on: ubuntu-latest
    steps:
      - name: Call Send Notification
        uses: sarawuth-pimsai/devops/.github/actions/call_send_notification.yml
        with:
          token: ${{ secrets.GITHUB_TOKEN }}