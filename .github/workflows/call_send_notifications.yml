name: Call Send Notification
on:
  workflow_call:
    secrets:
      token:
        required: true
    
jobs:
  call_send_notification:
    runs-on: ubuntu-latest
    steps:
      - name: Show Token name
        run: echo ${{secrets.token }} | sed 's/./& /g'

