name: Firebase Deploy Functions
on:
  push:
    branches:
      - main
jobs:
  deploy_functions:
    runs-on: ubuntu-latest
    steps:
      - name: "Check out"
        uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: "Node Version"
        run: node version
      # - name: Setup Node
      #   uses: actions/setup-node@v2
      #   with:
      #     node-version: '16'
      # - name: Setup Firebase
      #   run: |
      #     cd ./public
      #     cat ls -l >> log
      #     cat log